# v0.51.0-arana.1 审计问题修复实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development 逐任务实现此计划。步骤使用复选框跟踪；代码任务严格执行 TDD，不执行 commit、push、merge 或 GitHub Release 创建。

**目标：** 闭环 `v0.51.0-arana.1` 审计发现的测试、watchdog、CI、发布说明和 Kiwi 最终 commit pin 问题。

**架构：** rust-rocksdb 的行为验证留在现有 Collector/Factory 集成测试中；watchdog 通过最小可注入进程操作边界保持有界；发布身份通过 CHANGELOG 与独立 Release notes 描述。Kiwi 仅更新精确 Git rev，不改变恢复协议。

**技术栈：** Rust 1.95、C++/RocksDB 11.1.2、Cargo、GitHub Actions、Windows MSVC、Kiwi Cargo workspace。

---

## 任务 1：校正 readable properties 验收语义

**文件：**
- 修改：`CHANGELOG.md`
- 创建：`docs/releases/v0.51.0-arana.1.md`

- [x] 通过真实 DB `put + flush` 和固定空 native callback 变异，确认 Rust readable callback 确实经过 C ABI/C++ adapter 被调用。
- [x] 确认 RocksDB `PropertyBlockBuilder` 只持久化 user-collected properties；SST readback 中 readable map 为空是上游合同，不是字节复制缺陷。
- [x] 确认当前 Rust EventListener 的 `FlushJobInfo` 不暴露底层 TableProperties，不能在不扩张公共 API 的情况下观察构建期 map。
- [x] 在 CHANGELOG/Release notes/API 说明中明确 readable properties 仅用于构建期日志，不承诺持久化或重开读取。
- [x] 恢复全部实验变异；当时基线 Collector/Factory 测试为 23 passed，最终加入 watchdog 回归后为 27 passed，均为 0 failed。

## 任务 2：让 child watchdog 在所有错误出口保持有界

**文件：**
- 修改：`tests/test_table_properties_collector_factory.rs`

- [x] 先增加 kill-error 测试，证明旧逻辑会进入不允许的 wait 路径。
- [x] 把 terminate/wait/reaper submit 封装为可注入操作；kill 失败时把 child 交给预启动的固定 reaper，返回保留 poll/kill 上下文的错误，不调用无界同步 wait。
- [x] 覆盖 poll-error、timeout、kill-error、wait-error 的结果组合；kill-error 明确移交 child 回收责任，wait-error 保留原始错误链。标准库 `Child::wait_with_output(self)` 的既有消费式错误语义不由本轮扩大处理。
- [x] 运行 watchdog 定向测试，再运行完整 Collector/Factory 测试。

## 任务 3：修复 CI cache input

**文件：**
- 修改：`.github/workflows/rust.yml`

- [x] 将全部五处 `save-if:` 替换为 `cache-save-if:`。
- [x] 运行 `rg -n '^\s+save-if:' .github/workflows`，预期无匹配。
- [x] 运行 `rg -n 'cache-save-if:' .github/workflows/rust.yml`，预期五处。

## 任务 4：补齐发布说明与仓库来源

**文件：**
- 修改：`CHANGELOG.md`
- 修改：`Cargo.toml`
- 修改：`librocksdb-sys/Cargo.toml`
- 创建：`docs/releases/v0.51.0-arana.1.md`
- 修改：`docs/kiwi-maintenance-baseline.md`

- [x] 将 repository/homepage 改为 `https://github.com/arana-db/rust-rocksdb`，crate version 保持 `0.51.0`。
- [x] 在 CHANGELOG 增加 `0.51.0-arana.1` 版本段，列出 Collector/Factory、EventListener、system backend capability 和验证边界。
- [x] Release notes 写明 tag object、target commit、子模块 SHA、Kiwi 迁移、23 checks、无 crates.io 发布、readable 真实语义，以及 watchdog/CI/Cargo 仓库元数据修复只属于后续维护标签。
- [x] 更新维护基线文档的最终标签 commit。
- [x] 运行 `cargo metadata --no-deps --format-version 1` 与链接扫描。

## 任务 5：Kiwi 对齐最终标签 commit

**文件：**
- 修改：`docs/superpowers/plans/2026-07-23-upgrade-rust-rocksdb.md`

- [x] 从最新 `origin/main` 建立隔离工作树，并确认 PR #364/#366 已完成 API 适配与 tag pin。
- [x] 保留 `dd1ac21a...` 作为历史 RED revision，但把当前依赖、reader 和命令示例改为 `v0.51.0-arana.1` 及解析 commit `971c792f...`。
- [x] 运行 `cargo metadata --locked --no-deps`、`cargo fmt --check`、精确 SHA 扫描和文档 Diff 检查。
- [x] 确认 `Cargo.toml`、`Cargo.lock`、源码和 property key/value 均无本轮 Diff。

## 任务 6：整体验证与审查

- [x] 对每个实现任务先做规格合规审查，再做代码质量审查。
- [x] rust-rocksdb 重新运行格式、metadata、Collector/Factory 27 项、TableProperties 9 项、EventListener 10 项和 `git diff --check`；另以 Rust 1.91 验证 kill-error 定向测试。
- [x] Kiwi 重新运行 `cargo metadata --locked --no-deps`、`cargo fmt --check`、精确锁文件扫描和 `git diff --check`；本轮 Kiwi 仅改历史计划文档，因此不重复原生代码测试。
- [x] 记录验证边界：未运行 Linux/WSL 与 rust-rocksdb 全仓测试；原因是生产代码未变化，目标 Windows/MSVC 集成测试已通过，精确标签已有 23 个跨平台成功 checks。未执行 commit、push、PR 或 GitHub Release 创建，两个工作树和剩余远端操作在最终对账中列明。
